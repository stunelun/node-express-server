type getDataResponse {
    createdAt: String!
    updatedAt: String!
    currency_pair: String!
    last: Float!
    timestamp: Float!
    error: Boolean!
}

type Query {
    getData(name:String!): getDataResponse!
}